{% extends 'base.html.twig' %}

{% block body %}
    <h1>Categoriesannuaire</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categoriesAnnuaire.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categoriesAnnuaire.nom }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ categoriesAnnuaire.image }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('categoriesannuaire_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('categoriesannuaire_edit', { 'id': categoriesAnnuaire.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
