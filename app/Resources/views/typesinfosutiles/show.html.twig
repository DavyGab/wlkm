{% extends 'base.html.twig' %}

{% block body %}
    <h1>Typesinfosutile</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typesInfosUtile.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ typesInfosUtile.titre }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ typesInfosUtile.image }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('typesinfosutiles_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('typesinfosutiles_edit', { 'id': typesInfosUtile.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
